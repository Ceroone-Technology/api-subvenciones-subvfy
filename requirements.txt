fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# bcrypt fijado a 4.x a propósito: passlib 1.7.4 no arranca su backend con
# bcrypt >= 5.0 (falla en la deteccion de backend con "password cannot be
# longer than 72 bytes"). passlib[bcrypt] no pone techo, asi que sin este
# pin un build limpio instala bcrypt 5.x y el hashing de contraseñas revienta.
bcrypt==4.2.1
anthropic==0.42.0
# Cliente de Amazon SES para las notificaciones de alertas. En Lambda ya
# viene en el runtime, pero se fija igualmente para que local y CI coincidan.
boto3==1.43.100
apscheduler==3.10.4
